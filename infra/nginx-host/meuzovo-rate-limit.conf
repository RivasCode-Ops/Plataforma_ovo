# Rate limit — pedido publico do site (incluir em /etc/nginx/conf.d/)
# 20 req/min por IP + burst (alinha com backend ~15/h em memoria)

limit_req_zone $binary_remote_addr zone=meuzovo_pedido_site:10m rate=20r/m;
limit_req_status 429;
