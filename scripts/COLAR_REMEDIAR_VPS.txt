systemctl enable docker && systemctl start docker && cd /opt/Plataforma_ovo && git pull --ff-only && bash infra/scripts/vps-remediar-completo.sh 2>/dev/null || (cd /opt/Plataforma_ovo/infra && grep -q '^HTTP_PORT=' .env.prod && sed -i 's/^HTTP_PORT=.*/HTTP_PORT=8080/' .env.prod || echo 'HTTP_PORT=8080' >> .env.prod && docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build && sleep 15 && curl -s http://127.0.0.1:8080/api/health && curl -s http://app.granjauniao.com.br/api/health)
